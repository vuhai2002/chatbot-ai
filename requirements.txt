fastapi==0.103.1
uvicorn==0.23.2
python-multipart==0.0.6

# LangChain & OpenAI
langchain==0.0.286
langchain_community==0.0.10
openai==1.77.0
tiktoken==0.5.1          # Có sẵn wheel cho CP‑312, không cần Rust

# Xử lý tài liệu
pymupdf==1.25.5          # đọc PDF
python-docx==0.8.11      # đọc DOCX
textract==1.6.5          # trích xuất đa định dạng
# Firebase (nếu dùng)
firebase-admin==6.2.0

# Vector DB & LLM storage
chromadb==0.4.13
# faiss-cpu==1.7.4

# Database & cấu hình
psycopg2-binary==2.9.10
pydantic-settings
pydantic==2.4.2
python-dotenv==1.0.0

# Phụ trợ chung
six==1.17.0              # thỏa mãn textract & posthog